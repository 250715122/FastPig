# 坚果云同步 - 快速开始

## 一分钟配置

### Windows 用户

1. **找到坚果云根目录**（通常是 `C:\Users\你的用户名\Nutstore`）

2. **配置环境变量**（推荐，一次配置永久生效）
   ```
   控制面板 → 系统 → 高级 → 环境变量
   新建变量：
     名称：NUTSTORE_DIR
     值：  C:\Users\你的用户名\Nutstore
   ```

3. **或临时配置**（适合测试）
   ```bash
   # 启动前执行
   set NUTSTORE_DIR=C:\Users\你的用户名\Nutstore
   ```

4. **启动程序**
   ```bash
   java -cp "target/classes;target/dependency/*" com.gt.ModernSwingTest
   ```

## 使用方式

### 自动同步（无需操作）
- ✅ **启动时**：自动从云端拉取最新数据库
- ✅ **关闭时**：自动上传本地数据库到云端

### 手动同步
- 🔑 按 **Ctrl+Alt+S** 立即同步到云端
- 📊 底部状态栏显示同步状态

## 验证是否生效

1. 启动程序，观察控制台输出
2. 看到 `[DbSync] 已将本地数据库同步到云端` 表示同步成功
3. 检查 `<坚果云目录>\FastPig\fastpig.db` 文件是否存在

## 未配置时

如果不配置 `NUTSTORE_DIR`，程序会：
- ✅ 正常运行（本地单机使用）
- ⚠️ 控制台提示 `[DbSync] 未配置 NUTSTORE_DIR，跳过云端同步`
- ⚠️ 所有同步操作静默跳过

## 快捷键汇总

| 快捷键 | 功能 |
|--------|------|
| **Ctrl+Alt+S** | 🔄 同步数据库到坚果云 |
| Ctrl+S | 💾 保存当前笔记 |
| Alt+S | 🪟 打开编辑器（全局） |
| Alt+P | 👁️ 预览/关闭预览 |
| Alt+D | 🗑️ 删除笔记 |
| Alt+Z | ↩️ 撤销删除（1分钟内） |

## 多设备使用建议

1. **设备A** 编辑完成后关闭程序（自动上传）
2. 等待坚果云同步完成（云图标停止旋转）
3. **设备B** 启动程序（自动拉取最新）
4. 编辑完成后同样关闭程序

⚠️ **避免两台设备同时运行，防止冲突**

## 常见问题

### Q1: 如何确认坚果云目录？
A: 打开坚果云客户端，点击右上角齿轮 → 设置 → 查看同步文件夹路径

### Q2: 同步失败怎么办？
A: 检查：
- 坚果云客户端是否运行
- `NUTSTORE_DIR` 路径是否正确
- 是否有读写权限

### Q3: 数据被误覆盖如何恢复？
A: 坚果云自带版本历史，网页端右键文件 → 查看历史版本

### Q4: 能否自定义云端目录名？
A: 当前固定为 `<坚果云目录>/FastPig/`，后续版本可能支持配置

---

📖 详细文档：[SYNC_README.md](SYNC_README.md)  
🧪 测试报告：[TEST_REPORT.md](TEST_REPORT.md)
